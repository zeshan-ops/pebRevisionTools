# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Environment
.env
.env.local

# Exam material (PEB copyright) — keep local unless confirmed shareable.
# Delete the line below if the repo is private and you want the PDFs synced.
exam-materials/**/*.pdf

# Derived exam content (extracted question/mark scheme text is PEB copyright)
content/derived/

# Source .docx notes stay local; the parsed cards (content/notes/*.json) are
# tracked, since they are the user's own work.
content/notes/source/

# Python venv for the extraction scripts
.venv/

# Node / Next
node_modules/
.next/
*.db
*.db-wal
*.db-shm

# Claude Code local settings
.claude/settings.local.json
